(ns my-exercise.elections
  (:require [hiccup.page :refer [html5]]))

(defn election [e-params]
  ;; TODO hiccup template for one elections
  )

(defn elections [elections]
  (map election elections))

;; TODO Target these templates from the search#submit actions
